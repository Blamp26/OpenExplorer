[package]
name = "open-explorer-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
open-explorer-domain = { path = "../open-explorer-domain" }
open-explorer-engine = { path = "../open-explorer-engine" }
